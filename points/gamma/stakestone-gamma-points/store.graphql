type AccountSnapshot @entity {
  id: ID!
  network: String!
  vault: String!
  account: String!
  timestampMilli: BigInt!
  balance: BigInt!
}

type BeefyAccountSnapshot @entity {
  id: ID!
  network: String!
  vault: String!
  account: String!
  timestampMilli: BigInt!
  balance: BigInt!
}