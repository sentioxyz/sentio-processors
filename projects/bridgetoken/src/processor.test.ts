// NOTE: Tests disabled since the processor is temporarily disabled due to breaking SDK changes.
// See processor.ts for details.

import { describe, test } from 'node:test'

describe('Test Processor (DISABLED)', () => {
    test('processor is disabled', async () => {
        console.log('bridgetoken processor is disabled - needs refactoring for new SDK API')
    })
})
